name: Example Parity
loglevel: 0
run:
  - name: machine
    config:
      dest: testmachinedest
      composefile: docker-compose.yml
sync:
  - name: mirror
    config:
      verbose: false
      exclude:
        - tmp
        - \.log$
        - \.git
