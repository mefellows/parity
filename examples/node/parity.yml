name: Example Parity
loglevel: 2
run:
  - name: compose
    config:
      dest: testmachinedest
      composefile: docker-compose.yml
sync:
  - name: mirror
    config:
      verbose: false
      exclude:
        - tmp
        - \.log$
        - \.git
shell:
  - name: compose
